/*
Task: Find the Most Common Character in an Array of Strings

Constraints:

- The strings only contain alphabetic characters.
- The array is never empty.
- The array can contain between 2 and 10,000 strings (inclusive).

input: ["hello", "world", "hello"]
output: 'l'

*/

function mostCommonCharacter(strings) {}

const inputArray = ["hello", "world", "hello"];
console.log(mostCommonCharacter(inputArray));

module.exports = mostCommonCharacter;
